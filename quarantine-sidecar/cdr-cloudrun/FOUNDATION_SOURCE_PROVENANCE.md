# Foundation CDR Source Provenance

This directory was copied on 2026-08-27 from the user-owned, read-only audit checkout of `0ssol1620-byte/tavonel-compiled-world-activation` at immutable commit `e017cb65b8dd0a666740aa53a671a4ae10171dda`.

The original repository was not edited. The Foundation copy changes only the Cloud Build bootstrap project identifier from `tavonel-knowledge-compiler` to `tavonel-saas-foundation` and clarifies Foundation-specific readiness language. It contains no runtime secret, credential, account access key, or production configuration value.

Before the service can process even a harmless fixture, its local contract suite, dedicated secret bootstrap, Cloud Run deployment policy, health state, signed-request rejection behavior, and cleanup must be separately evidenced. Customer intake remains disabled until the full R2/sidecar path is independently qualified.
