# Locked compile cut 2

`compile-cut-2.mp4` is the frozen **18.00s** four-up. It sits in the **middle of the site** when we wire it. Cut 1 (`compile-cut.mp4`) stays the header cut.

- Camera: off, wide
- No bottom caption
- Col 1 NODES: cut-1 field, one live node (size/brightness only — **no halo**)
- Col 1 selected node = the node col 4 is linking **right now**
- Col 2 MARKDOWN: that node's markdown
- Col 3 top: Turtle/OWL (`ontology.ttl`, `@prefix`, `owl:Class`). Not yaml. Not `N/N`
- Col 3 bottom: dark TBox `owl:Class` cards, **no connector lines**, 4×2 filling the pane, fixed card height, escalator, highlight walks cells from t=0
- Col 4 WORLD: edges grow through **17.2s** so this column does not finish first

Do not retune `opening-film-2.tsx` layout against this file unless the lock is lifted.
