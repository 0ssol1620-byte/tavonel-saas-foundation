# Cut 2 — node → ontology → edges

Does not replace `compile-cut.mp4`. Cut 1 stays locked (nodes only, no edges).

Four-up, camera off, same TAVONEL Sources window.

1. **DRIVE / WORLD nodes**  
   The nodes made in cut 1 are already there. One node is selected and lights up.

2. **ORIGINAL / payload**  
   The markdown stored in that node.

3. **EXTRACT — two locked panes (heights do not move)**  
   - Top: rewrite to the ontology spec.  
   - Bottom: a correlation diagram of what that rewrite implies.

4. **WORLD**  
   While 3 runs, related nodes in this column grow edges. Linking is this cut, not cut 1.

No Finder chrome. No bottom caption band unless asked.
