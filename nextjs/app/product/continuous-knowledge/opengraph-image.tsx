import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Continuous recompilation", "Eight clauses that say what a compile guarantees when a source changes, each carrying the state it holds in this deployment.");
