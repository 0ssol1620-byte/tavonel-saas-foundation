import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Document understanding", "Reading scans and complex layouts while keeping the location of every region and the uncertainty around it for review.");
