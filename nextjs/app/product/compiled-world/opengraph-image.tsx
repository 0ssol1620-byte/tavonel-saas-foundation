import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Compiled World", "Objects, relations, evidence and versions, portable as one signed package.");
