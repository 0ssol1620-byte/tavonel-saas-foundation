import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Changelog", "What changed, grouped as added, improved and fixed, with the surface it touched and any breaking change.");
