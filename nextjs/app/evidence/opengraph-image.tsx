import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Evidence", "How a compiled result stays bound to the source version and the exact location inside it that it came from.");
