import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Supported sources", "Every source format TAVONEL reads, its support tier, what survives into the Compiled World, and the limits that come with it.");
