import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Subprocessors", "Every third-party service permitted to process TAVONEL account, document, billing or inquiry data, and what each one receives.");
