import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard(
  "Signed product path",
  "A public synthetic sample from source revision through review, activation, evidence-bound answer and signed export.",
);
