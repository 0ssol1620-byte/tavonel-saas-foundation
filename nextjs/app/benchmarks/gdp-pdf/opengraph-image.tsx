import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("GDP.pdf: compiled context vs the PDF alone", "A two-arm run on the public GDP.pdf task set, published whichever way it came out. Not comparable with published GDP.pdf scores.");
