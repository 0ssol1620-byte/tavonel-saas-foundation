import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Knowledge compilation benchmark", "The metric families, receipt fields and qualification rules a result has to satisfy before TAVONEL publishes it as a number.");
