import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Developers", "Compile knowledge once, then reach it live over MCP and the API, or take it away as a signed portable package.");
