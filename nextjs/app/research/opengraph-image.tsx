import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Research", "The open problems in compiling documents into evidence-bound, versioned knowledge, and how we work on them.");
