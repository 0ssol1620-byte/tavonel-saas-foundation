import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Research notes", "What we measured and how: the figures, their denominators, their receipts, and the hypotheses that did not hold.");
