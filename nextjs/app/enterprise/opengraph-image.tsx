import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Enterprise", "Identity, governance, audit, residency and operating economics for TAVONEL organizations.");
