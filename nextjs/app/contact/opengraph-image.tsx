import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Talk to TAVONEL", "Tell us what your documents need to become, without sending the documents themselves.");
