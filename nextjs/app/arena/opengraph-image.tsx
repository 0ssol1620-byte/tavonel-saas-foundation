import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Arena", "Reproducible document-AI research across parsing, retention, routing, cost and latency, with versions and failures attached.");
