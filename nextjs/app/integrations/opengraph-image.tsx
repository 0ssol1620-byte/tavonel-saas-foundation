import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Integrations", "Connect read-only source systems and compile their knowledge into a traceable World.");
