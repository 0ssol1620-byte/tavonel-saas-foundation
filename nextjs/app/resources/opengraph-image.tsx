import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Resources", "Explore a compiled world, read the documentation and API, and inspect the research and evidence behind the compiler.");
