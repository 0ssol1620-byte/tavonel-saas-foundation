import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Trust Center", "One index of everything TAVONEL publishes about how it handles documents, and the questions it has not answered yet.");
