import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Pricing", "Plans and measured compute for the TAVONEL Knowledge Compiler. Hard spend limits.");
