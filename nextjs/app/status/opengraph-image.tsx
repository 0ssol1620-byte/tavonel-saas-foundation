import { ogCard } from "@/lib/og-card";

export { alt, contentType, size } from "@/lib/og-card";

export default ogCard("Service status", "Component-by-component state of the active deployment, the last synthetic probe result, and where to report service impact.");
