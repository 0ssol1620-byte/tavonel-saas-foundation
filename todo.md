# Project TODO

- [x] Preserve strict isolation from existing `tavonel` production and the CDR activation repository; document non-live activation boundaries.
- [x] Codify disabled live intake, CDR, OCR/GPU, and candidate promotion states in shared server-consumed policy code.
- [x] Define tenant domain models for profiles, workspaces, memberships, plans, entitlements, document metadata, sanitization proofs, and candidate knowledge-graph metadata.
- [x] Implement server-side tenant authorization helpers and fail-closed entitlement/quota evaluation contracts.
- [x] Create Supabase migration and RLS policy package covering every browser-exposed table, including explicit allow and deny test cases.
- [ ] If a branch-capable Supabase plan is separately approved, execute the table-by-table RLS allow/deny matrix against that disposable test database before enabling browser sign-up.
- [x] Run the alternative main-project synthetic RLS probe only inside a single transaction that ends in `ROLLBACK`, after explicit confirmation that no persistent fixture rows may remain.
- [ ] Configure dedicated foundation Supabase email and Google OAuth redirect settings only after contextual approval of the exact foundation origin, OAuth client, consent configuration, and secret-handling boundary.
- [x] Create the approved dedicated Supabase Seoul project in `Phillip's projects`, verify its identity and empty state, and record only nonsecret metadata.
- [x] Apply a reviewed follow-up Supabase migration that revokes public SECURITY DEFINER execution and makes the internal billing ledger?셲 default-deny RLS policy explicit.
- [x] Re-run Supabase security advisor after the remediation and record the results without exposing credentials or user data.
- [x] Record the final clean Supabase security-advisor result in a nonsecret project qualification document.
- [x] Apply explicit restrictive default-deny RLS policies to credit ledger and GPU reservation tables, then verify a clean Supabase security advisor result.
- [ ] Configure a dedicated Paddle sandbox catalog, webhook destination, and server-only sandbox secrets; keep all live checkout and production billing disabled.
- [x] Design a credit-first, GPU-cost-protected product catalog with subscription access tier, prepaid credit packs, hard per-job and per-tenant spending caps, and margin floor telemetry.
- [x] Implement server-side credit catalog, pre-dispatch reservation, hard cap evaluation, and margin-floor dispatch pause contracts with regression tests.
- [x] Wire margin-floor breach into an explicit server-side GPU dispatch pause decision and regression test.
- [x] Implement a server-side GPU credit reservation and settlement contract with idempotency, release, and fail-closed behavior tests.
- [x] Apply the reviewed credit ledger and GPU reservation schema migration to the confirmed empty Seoul foundation project, then verify RLS remains enabled.
- [x] Document Paddle sandbox product/price mapping and event-to-credit ledger projection rules without configuring a live catalog.
- [x] Add the approved prepaid credit packs and no-unlimited-GPU messaging to the public pricing experience while keeping all checkout controls in presentation mode.
- [ ] Configure only the foundation-specific APAC R2 signer boundary after contextual approval of its scoped credential model, capability exposure, and non-residency disclosure; keep customer intake disabled.
- [x] Complete the provider-independent security checklist, test evidence, and activation-ready handoff while external provider identity and secret configuration remain unavailable.
- [x] Build a server-only Supabase Auth integration seam for email sign-up and Google OAuth, with personal workspace provisioning design and no credential fallback.
- [x] Build an elegant public SaaS landing and pricing experience with a clear sign-up path, capability boundaries, and responsive accessible layout.
- [x] Build an authenticated workspace shell with document intake status, quota visibility, knowledge candidate state, and explicit inactive CDR/OCR/GPU status.
- [x] Implement Paddle Checkout launch contracts with server-controlled price allow-listing and no live payment activation.
- [x] Implement raw-body Paddle webhook signature-verification, event-id deduplication, occurred-at ordering, and entitlement projection contracts.
- [x] Implement a tenant-scoped, short-lived browser-direct R2 quarantine upload capability contract that never proxies document bytes through Vercel or the database.
- [ ] Keep AV, CDR, OCR/GPU dispatch, candidate promotion, and live customer intake fail-closed until separate synthetic qualification and explicit approval.
- [x] Prepare the triggerless Seoul CDR bootstrap final-approval package from the verified Developer Connect source, fixed commit, YAML hash, and minimum privilege boundary without submitting a build.
- [x] Add Vitest coverage for authorization denials, entitlement/quota gates, billing event ordering/deduplication, upload capability denial, and bootstrap preflight immutability.
- [x] Run typecheck and test suite, visually verify desktop and mobile UI, and record remaining external integration approvals.
- [x] Produce a separate Next.js + Vercel deployment package that preserves the same fail-closed tenant, billing, and upload contracts without deploying it.
- [x] Publish a detailed nonsecret continuation handoff for the next Manus session, including verified evidence, safety gates, and exact next actions.
- [x] Create a new private GitHub repository containing only the TAVONEL SaaS Foundation source and nonsecret handoff materials, then verify the remote commit for cross-account continuation.
- [x] Replace the self-invalidating static GitHub handoff commit reference with source-of-truth `main` verification guidance for future cross-account clones.
- [x] Clarify that a fresh GitHub clone uses `origin` for this private repository, while only the original managed workspace has a separate artifact `origin` and `github` handoff remote.
- [x] Re-run 짠9 parent and Next.js local verification, then inspect desktop/mobile prepaid-credit UI while retaining every external provider and live data path in fail-closed state. (Parent: 13 files/27 tests, typecheck, build, diff check; Next.js: 2 files/2 tests, typecheck, explicit production build; desktop/mobile credit UI and presentation-only controls verified.)
- [x] Audit every currently fail-closed capability against source and provider readiness, separating code-only completion from actions requiring contextual external approval. (`shared/activationPolicy.ts`, provider status, and safe contracts audited; all four live paths remain disabled.)
- [x] Define and test a signup trial-credit policy with explicit abuse controls, expiry, entitlement boundaries, and margin guardrails; do not issue real credits until a verified billing/auth path exists. (2 credits, 7 days, one job; verified identity and identity/workspace one-time checks; 3 Vitest cases.)
- [x] Validate the published subscription and prepaid-credit economics against documented provider-cost assumptions and record a recommendation with sensitivity bounds. (`docs/TRIAL_CREDIT_AND_PRICING_DECISION_2026-08-27.md`; Scale floor economics and 30% all-in circuit breaker retained.)
- [x] Implement and test the remaining provider-independent signup, entitlement, upload-state, CDR-proof, receipt, and human-review contracts without enabling external provider calls. (Trial, upload capability/completion, CDR proof, GPU receipt, human review, object-key hardening, and MIME-normalization contracts are covered by source-only regression cases.)
- [ ] Obtain and document separate contextual approval before any foundation Auth, Paddle, R2 signer/CORS, CDR, RunPod, deployment, customer-data, or paid resource action.
- [ ] Run a customer-visible end-to-end test only after every upstream provider qualification is independently evidenced and a limited-pilot activation is approved.
- [ ] Execute the approved synthetic-only qualification sequence: document provider access and cost scope, then prove foundation R2 quarantine, CDR sanitization evidence, GPU signed receipt, and candidate-ready handoff without customer bytes, payment, or production changes.
- [x] Resolve CDR qualification blocker before sanitization: `/health` currently returns `CDR configuration is not qualified`; inspect the exact bootstrap evidence path without modifying production or activation repositories. (Foundation-only deployment, HMAC bootstrap, health/auth probes, logging exclusion, and one signed harmless fixture qualification completed; production CDR unchanged.)
- [x] Copy the pinned PDF-raster CDR implementation from activation commit `e017cb65` into the Foundation repository, replace only project identifiers in its deployment bootstrap, and run its fixture-only local contract suite before any new Cloud Run deployment. (Foundation copy provenance recorded; 10 harmless-fixture tests passed.)
- [x] Create a separate `TAVONEL SaaS Foundation` Google Cloud project, distinct from `tavonel-knowledge-compiler`, and prepare only min-zero/max-one synthetic CDR qualification resources within the approved boundary. (Initial project state had no API/billing/service/source connection; subsequent approved work linked Foundation-only billing, enabled required APIs, created only the Foundation HMAC secret, and deployed one isolated synthetic CDR service.)
- [x] Create no more than one `asia-northeast3` min-zero/max-one Foundation Cloud Run CDR service for harmless synthetic qualification, only after a separate final deployment confirmation; do not attach, alter, or inspect the existing production project billing configuration. (One user-approved service, `tavonel-cdr-synthetic`, was deployed only to the Foundation project.)
- [x] Verify the deployed Foundation CDR Cloud Run service configuration: fixed min/max/concurrency/CPU/memory/timeout/generation/access-log/HMAC-reference, then run only the approved harmless health and bad-HMAC readiness probes before any synthetic sanitize request. (Deployment settings reviewed; health is HTTP 200; missing, bad, and expired HMAC zero-byte probes are HTTP 401 before renderer/body processing; `_Default` request-log exclusion is enabled.)
- [x] Create and verify only a Foundation CDR service-scoped Cloud Logging exclusion for Cloud Run request logs, preserving admin/audit and application error logs; do not use a broad project logging disablement. (Read-only `_Default` sink metadata shows enabled `tavonel-cdr-synthetic-request-logs` with exact Cloud Run service and request-log-name filter.)
- [x] Run exactly one provider-internal, harmless, validly signed CDR synthetic fixture against `tavonel-cdr-synthetic`; keep the HMAC inside Secret Manager/Cloud Build, record only response headers/digests/exit result, and never send customer content or persist the response body. (Build `acb51e28` returned HTTP 200 clean image-only PDF with 806-byte input, 10717-byte output, matching digest evidence, and temporary-file cleanup; no customer data.)
- [x] Verify the user-completed Foundation billing/quota remediation read-only, then resume only the Foundation Cloud Run CDR synthetic qualification path if billing and API propagation are both confirmed. (Billing attached only to Foundation; Cloud Run Admin API enabled; the isolated CDR service now exists and its qualification evidence is recorded.)
- [x] Submit and track the existing billing account's project-quota increase request for `tavonel-saas-foundation`; do not migrate or detach production billing while waiting for approval. (Google Trust & Safety submission confirmation received; review is stated as typically about two business days.)
- [ ] Under the approved $5 USD ceiling, perform only Foundation-targeted synthetic qualification: one harmless R2 marker write/head/delete and at most one non-SSH, min-workers-zero GPU qualification; never replay ambiguous or 429 paid writes.
- [x] Add and test a provider-independent RunPod synthetic-qualification policy that requires read-only capacity evidence, one-shot mutation, no SSH/public key path, port 8001 health, min workers 0, volume 0, 80 GiB container disk, and cumulative spend at or below $5 before any endpoint call is permitted. (Implemented in `shared/runpodSyntheticQualification.ts`; no provider call.)
- [x] Record the RunPod synthetic-qualification regression result. (4 regression cases; Foundation test suite now 15 files/34 tests.)
- [x] Enable the approved Cloudflare OAuth connector, verify only the designated Foundation bucket read-only, and record its isolation from production before requesting the synthetic marker write/head/delete. (`docs/SYNTHETIC_R2_QUALIFICATION_2026-08-27.md`)
- [x] Execute the already approved Foundation R2 synthetic marker PUT?묰ET?묭ELETE as one sequence; record outcome without body or credential disclosure and halt rather than replay on any ambiguous result. (HTTP 200 for each operation; marker deleted; no GPU spend.)
- [x] Add and test a provider-independent document processing state machine that binds CDR proof digests to a quarantined document, binds a GPU receipt to the same proof, and allows only an authorized human reviewer to approve or reject a candidate while global promotion remains disabled. (4 regression cases; source-only metadata binding with no CDR/GPU/R2 call.)
- [x] Harden CDR proof and candidate receipt object-key validation to reject traversal-like, empty-segment, control-character, and cross-workspace metadata paths before any provider adapter exists. (Regression assertions added; no provider adapter or byte access.)
- [x] Add and test a metadata-only quarantine completion contract that accepts a browser-direct upload only when bounded capability, exact object key/length, source digest, and declared/observed MIME match; retain global intake fail-closed. (3 regression cases; no R2 request or byte access.)
- [x] Preserve the normalized CDR-qualified MIME in every future upload capability so post-upload observed MIME comparison cannot reject valid MIME parameters such as `application/pdf; charset=binary`. (Capability now uses the validated normalized MIME rather than raw browser metadata.)
- [x] Read the newly reported Gmail response for the Foundation Google Cloud billing project-quota request and record only its nonsecret approval state and conditions before any provider mutation. (Quota Granted; Google states propagation may take up to one hour; console still shows Foundation billing disabled and unlinked.)
- [x] Read the Gmail thread `Re: Cloudmersive Sales Inquiry #633a291f-7477-4a44-9592-4dd85af49a4a` and record a nonsecret relevance decision; do not create an account, accept terms, or send a reply without a separate approval. (Relevant only as a possible future third-party CDR option; vendor claims require separate DPA/region/retention/security/pricing validation.)
- [x] After the granted quota has propagated, connect the existing billing account only to `tavonel-saas-foundation`; do not change any other project or billing attachment, then verify the resulting Foundation status read-only. (User-approved Foundation-only attachment completed; console confirms linked account.)
- [x] Enable Cloud Run Admin API only in `tavonel-saas-foundation`, then verify its enabled status and that no Foundation Cloud Run service has been created. (Console reports `run.googleapis.com` enabled; Cloud Run overview remains empty.)
- [x] Execute one Foundation-only CDR HMAC bootstrap from the reviewed local source after preflight, without exposing secret material; verify only secret metadata and preserve the no-service state. (Foundation-only Cloud Build succeeded; Secret Manager lists one `tavonel-cdr-hmac` version in `asia-northeast3`; the later isolated CDR service references it and qualification evidence is recorded.)
- [x] Complete the user-approved Cloud Shell credential authorization only for the active Foundation project before executing the reviewed HMAC bootstrap command. (User completed the embedded console authorization; no credential material was read or stored.)
- [x] Record the RunPod connector OAuth callback failure (`redirect_uri is not allowed`) and keep the connector disabled until an approved callback-capable authentication path is available; do not create an endpoint or spend GPU budget. (Hosted MCP OAuth rejected the callback; official docs confirm API-key override/local MCP as alternatives.)
- [x] Register a new RunPod API-key MCP connector with the credential stored only in the connector's server-side bearer header, then use it only for read-only capacity preflight; do not create or mutate GPU resources. (Enabled server key `runpod-foundation-read-only` resolved the CLI lookup issue; list-gpu-types succeeded; no GPU resource was created.)

- [x] Reconcile the 2026-08-28 Final Productization Masterplan with the Foundation handoff and preserve all existing fail-closed boundaries; do not merge repositories or enable live customer/auth/billing/R2/GPU paths.
- [x] Audit the latest `ai-knowledge-compiler`, `tavonel-compiled-world-activation`, and `tavonel-saas-foundation` repository states and classify their assets as KEEP, PORT, REPLACE, or ARCHIVE without modifying production or activation repositories. (Read-only snapshots: core `bd0fb334`, activation `e017cb65`, Foundation `1e38acc`; no donor mutation.)
- [x] Define and record canonical responsibility boundaries: Core Engine for CIR/identity/diff/dependency/recompilation/world semantics, Product Platform for tenant/auth/billing/R2/control-plane/UI, and activation/foundation repositories as migration donors until porting is verified. (`docs/CANONICAL_RESPONSIBILITY.md`, `docs/PRODUCT_CONVERGENCE_AUDIT_2026-08-28.md`)
- [x] Specify a versioned Product-to-Core compile job envelope, worker boundary, receipt/artifact contract, tenant binding, digest lineage, and atomic candidate-world promotion semantics as source-only contracts. (`shared/productCoreCompileEnvelope.ts`; 5 Vitest cases; no bytes/provider calls.)
- [x] Prepare a staged migration inventory for Google Drive/connectors, R2 immutable gates, RunPod release/receipt safety, `/world` UI language, CDR evidence, Supabase/RLS, Paddle, credits, entitlements, and abuse/cost controls; keep all external mutations and live data paths disabled. (`docs/MIGRATION_INVENTORY_2026-08-28.md`)
- [x] Run repository-wide typecheck/tests/build and documentation consistency checks after the product-convergence records are added, then commit only the isolated Foundation changes to its private repository. (Root: 18 files/46 tests, typecheck, Vite+server build; Next.js: 2 files/2 tests, typecheck, production build passed with `NODE_ENV=production`.)

- [x] Produce a complete continuation handoff for a fresh session, including repository map, exact commits, provider qualification evidence, current fail-closed flags, secrets policy, completed commands, remaining work, prohibited actions, and restart/verification commands. (`docs/HANDOFF_FINAL_PRODUCTIZATION_2026-08-28.md`, 656 lines; no known secret patterns.)
- [x] Reconcile the handoff against the latest Foundation source tree, todo history, provider status, product-convergence audit, migration inventory, and GitHub remote state. (Required sections and referenced files validated.)
- [x] Commit and push the complete handoff and all current Foundation changes to the isolated private GitHub repository, then verify the remote commit and clean working tree. (Handoff/evidence sync commit `0f3ea8506424e95030b0086e1b6ebe2e67c883ca` pushed to private `origin/main`; final metadata commit follows.)
- [x] Attach the final handoff Markdown and key supporting documents so a new worker can resume without requesting secrets or reconstructing prior context. (RunPod raw nonsecret evidence and repository audit summary are stored under `docs/evidence/`.)

- [x] Add a versioned Product-to-Core field map (`shared/productCoreFieldMap.ts`) from Core `bd0fb334` CIR/world/identity/receipt symbols without importing Python; unknown Core fields fail closed; unmapped symbols are explicit TODOs with file+symbol evidence.
- [x] Add Product-owned candidate-world metadata, manifest digest, validation/equivalence, parent pointer, and atomic promotion contracts (`shared/candidateWorldContract.ts`) while keeping `activationPolicy.candidatePromotion.enabled === false`.
- [x] Unify quarantine completion and immutable object-key proof into a metadata-only adapter (`server/foundation/immutableObjectProofAdapter.ts`) that reuses key hardening and MIME normalization and never reads bytes or calls R2.
- [x] Record Activation RunPod release/receipt file-level provenance for a later port (`docs/PORT_PACKAGE_RUNPOD_RECEIPT_2026-08-28.md`) without copying production trees.
- [x] Add P0 vertical-slice acceptance matrix, convergence/architecture pointers, and synthetic world v1/v2 + zero cost-ledger fixtures.

