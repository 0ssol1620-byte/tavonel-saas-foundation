# Canonical architecture

**Status:** Pointer. Do not duplicate the freeze.

The authority model (Core Engine vs Product Platform, donor repos, promotion boundary, provider fail-closed rule) is canonical in:

- `docs/CANONICAL_RESPONSIBILITY.md`

Product–Core compile envelopes: `shared/productCoreCompileEnvelope.ts`  
Cross-language field map: `shared/productCoreFieldMap.ts`  
Candidate/atomic promotion: `shared/candidateWorldContract.ts`
