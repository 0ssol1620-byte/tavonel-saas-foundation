# Foundation R2 signer token — 2026-08-28 KST

A Cloudflare R2 API token named `tavonel-saas-foundation-r2-signer` was created with Object Read & Write on `tavonel-saas-foundation-quarantine` only. It was stored as Vercel environment variables on project `tavonel-saas-foundation` (Phillip's projects), Production + Preview: `R2_ACCOUNT_ID`, `R2_BUCKET`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`. Secret values are not recorded here.

Not done in this step: production `tavonel` Vercel project, all-bucket tokens, public bucket access, customer intake, redeploy, object canary from the browser origin.

Customer intake remains disabled until a synthetic signer canary and remaining Auth/Paddle gates are independently evidenced.
