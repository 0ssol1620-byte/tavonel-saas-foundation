# TAVONEL SaaS Foundation — Continuation Handoff

**Prepared by:** Manus AI  
**Status date:** 2026-08-28 (KST)
**Scope:** Isolated multi-tenant document-SaaS foundation, its dedicated Supabase project, nonsecret provider readiness records, and an entirely separate CDR bootstrap approval package.

> **Operating principle:** The foundation is designed to be useful before it is live. It currently exposes only presentation and decision contracts; it does **not** accept customer document bytes, create payment sessions, run CDR/OCR/GPU work, or promote knowledge candidates. Every one of those paths must remain fail-closed until its own synthetic qualification and contextual approval are complete.

## 1. Executive continuation summary

This workspace implements a polished TAVONEL SaaS foundation without altering the pre-existing production application or its isolated CDR activation repository. The primary managed project is `/home/ubuntu/tavonel-saas-foundation`, a React/Vite/Express/tRPC foundation with a separate `nextjs/` App Router package intended for a future Vercel Seoul deployment. The user wants an eventual multi-tenant, governed workflow of drag-and-drop document intake, quarantine storage, sanitization, automated candidate extraction, human review, and only then knowledge/world promotion.

The data-path design is intentionally split: **Supabase Postgres** owns identity, workspace metadata, authorization, entitlements, document metadata, sanitization proof metadata, billing-event state, credits, and job reservations; **Cloudflare R2** is reserved for the document bytes. Vercel/Next.js and Postgres must never proxy or store customer document bytes. The policy source `shared/activationPolicy.ts` keeps customer intake, CDR, OCR/GPU, and candidate promotion disabled.

A real but empty dedicated Supabase project has been created and qualified. The project is `tavonel-saas-foundation` (`tfcorhjkqcuisqhsjemz`) in Seoul `ap-northeast-2`; it is `ACTIVE_HEALTHY`. Four reviewed schema/hardening migrations have been applied, the 13 public tables were observed RLS-enabled with zero rows, a synthetic A/B tenant probe passed only inside an explicitly rolled-back transaction, and the final Supabase Security Advisor result was `lints: []`. These are **database-foundation facts**, not approval to enable Auth, browser signup, uploads, payment, or processing.

No Foundation-specific WebDev checkpoint has been created. The private Foundation repository `main` is the source of truth and must be clean, fully verified, and pushed before any future checkpoint is considered. The managed TAVONEL production checkpoint history is out of scope and must not be used for this Foundation. Before any external activation, the follow-on session must first run the documented verification commands, review `todo.md`, and inspect the current remote `main`. The task is deliberately unfinished.

| Area | Current state | What is intentionally absent |
|---|---|---|
| Foundation UI | Public landing, pricing, workspace shell, and explicit four-gate status display are implemented | No live signup, checkout, upload, or processing action |
| Next.js/Vercel package | Separate App Router package exists; package-local production build previously passed | No Vercel project linkage or deployment |
| Supabase | Dedicated Seoul project; 4 migrations applied; clean final advisor | No Auth provider, redirect URI, public config injection, real user, or secret usage |
| Paddle | Contract code and pricing/credit mapping docs exist | No vendor account/session, catalog, price, notification endpoint, signing secret, or checkout |
| R2 | Dedicated `tavonel-saas-foundation-quarantine` bucket passed one harmless marker PUT → GET → DELETE, with cleanup recorded | No signer, key, Worker binding, CORS rule, public endpoint, or browser capability; this is not customer-intake qualification |
| GPU/RunPod | Credit-reservation, margin-pause, and one-shot synthetic qualification policy contracts exist | No connected account, endpoint, credential, request, dispatch, or spend |
| CDR | Foundation-local pinned source copy passed 10 harmless fixture tests; Foundation billing, Cloud Run/Secret Manager/Cloud Build APIs, and a one-shot Seoul-replicated HMAC secret are qualified | No Cloud Run service, Artifact Registry artifact, CDR request, runtime secret attachment, or retry automation |

## 2. Non-negotiable isolation and safety constraints

The separate foundation must not modify, deploy, reconfigure, or otherwise touch the existing `tavonel` production application, its `ai-knowledge-compiler` source, production Vercel configuration, production Cloudflare resources, existing archive hashes, unrelated worktrees/branches, or the normal CDR source trigger. The production repository and activation repository are evidence sources only unless the user gives a narrowly contextual, final approval for a separate CDR action described below.

Customer content is not test data. Do not fabricate customer reviews, ratings, testimonials, identities, documents, payment events, jobs, or source objects. Before customer intake is independently approved, tests must use code-only contracts or harmless synthetic records inside transactions that explicitly roll back. Do not use free/public scanners, arbitrary third-party OCR services, or unapproved APIs with customer bytes. Do not add polling, schedules, auto-retry loops, or background workarounds.

The following material must **never** be requested, printed, read, copied, reused, committed, inserted into a command line, placed in a document, or moved into source code: database passwords, Supabase service-role keys, OAuth tokens/codes/client secrets, Paddle/Cloudflare/RunPod/Vercel keys, R2 credentials, HMAC values, service-account keys, OTPs, and the value that surfaced in a discarded earlier Supabase browser draft. That draft was canceled. It is compromised by exposure and is permanently out of scope; never attempt recovery or reuse.

> **Fail-closed rule:** A user’s broad instruction to “continue” or “give all permissions” does not waive a contextual confirmation for a new paid plan, merchant/KYC terms, credential creation, OAuth client creation, runtime secret attachment, CORS/network change, deployment, payment, or use of real customer documents/GPU.

## 3. Current architecture and intended runtime boundary

The foundation’s eventual target architecture is **Next.js on Vercel**, **Supabase Auth/Postgres/RLS**, **Paddle Billing**, and **Cloudflare R2**. This must not be confused with the managed-project scaffold, which remains React/Vite/Express/tRPC with a template MySQL connection that is not the product-domain datastore. Product-domain Supabase SQL lives in `supabase/migrations/`; do not migrate it to the scaffold database.

| Layer | Foundation responsibility | Current enforcement / design |
|---|---|---|
| Browser/UI | Show workspace, entitlement, pricing, and readiness state | Controls notify the user that a provider-qualified backend is required; no browser action can open a live path |
| Next.js/Vercel | Future public site and safe route host in Seoul `icn1` | Package exists under `nextjs/`; API routes explicitly return no-store/fail-closed readiness |
| Supabase Auth | Future email and Google identity | Server-side readiness contract denies invalid/missing configuration; no provider is configured |
| Supabase Postgres/RLS | Tenant metadata, roles, entitlements, proof/candidate metadata, billing and credit state | Reviewed SQL applied to the dedicated Seoul project; tables are empty and RLS-enabled |
| Paddle | Future sandbox-first billing events and entitlement/credit projection | Raw-body HMAC, idempotency, and ordering contracts; client price IDs are `null` |
| Cloudflare R2 | Quarantine bytes only | Separate APAC bucket; one cleaned-up harmless marker proves bucket control-plane access, but no signer, credential, public endpoint, or browser upload URL |
| CDR/AV/OCR/GPU | Only qualified sanitized bytes can create candidates | All policy flags remain `false`; no external dispatch capability is available |
| Review/promotion | Human governance before canonical knowledge state | Candidate/world promotion remains globally disabled |

The file path must remain browser-direct only after future qualification: browser → short-lived, tenant-scoped R2 signed `PUT` → quarantine object. Application/API servers must issue a bounded capability but must never relay the bytes. Supabase receives only document metadata, permission/entitlement state, checksum or integrity references as appropriate, and later sanitization/candidate metadata.

## 4. Verified Supabase evidence

The exact dedicated project target is `tfcorhjkqcuisqhsjemz` in organization `Phillip's projects`, Seoul `ap-northeast-2`, state `ACTIVE_HEALTHY`. It was empty before migrations. This is the **only** Supabase project this foundation may target. Do not use an old production-related project, its OAuth credentials, or any provider-generated credential from an abandoned screen.

| Migration source file | Applied effect | Evidence / safety outcome |
|---|---|---|
| `0001_tavonel_tenant_foundation.sql` | Tenant profiles, workspaces, memberships, plans, entitlements, document/proof/candidate metadata, Paddle metadata/events, default RLS | 11 initial public tables, all observed RLS-enabled |
| `0002_credit_ledger_and_gpu_reservations.sql` | Internal credit ledger and GPU-reservation persistence model | Expanded public-table count to 13; no live credit writer or dispatcher was enabled |
| `0003_harden_rls_function_exposure.sql` | Non-exposed membership helper and revocation of public `SECURITY DEFINER` execution; explicit billing deny | Post-hardening advisor previously returned no lints |
| `0004_harden_credit_ledger_rls.sql` | Explicit restrictive default-deny policies for ledger/reservation command state | Final post-hardening advisor result was `lints: []` |

The expected public relation inventory is `profiles`, `workspaces`, `workspace_memberships`, `plans`, `workspace_entitlements`, `documents`, `sanitization_proofs`, `knowledge_graph_candidates`, `paddle_customers`, `paddle_subscriptions`, `billing_events`, `credit_ledger_entries`, and `gpu_job_reservations`. All last observed tables were RLS-enabled and had zero rows.

The completed A/B probe used synthetic identities and one SQL transaction with mandatory `ROLLBACK`. It returned `rls_matrix_passed: true`, `persistence: transaction_rolled_back`, and `persisted_fixture_rows: 0`. It did not create users, document objects, OAuth configuration, payment state, or processing work. The dedicated branch method was attempted only after a provider quote was approved, but Supabase rejected creation because branching requires Pro. **No plan upgrade, branch, or payment occurred.** The fuller pgTAP matrix remains in `supabase/tests/tenant_rls_matrix.sql` for a future explicitly approved disposable test database.

The helper `scripts/prepare-supabase-migration.mjs` currently writes a fixed migration name (`tavonel_tenant_foundation_0001`) even when invoked for later SQL. A future session should first use read-only migration history inspection to establish how the provider recorded migration identifiers. Do not mutate the provider merely to cosmetic-rename history. If future DDL is approved, fix the helper to accept an explicit name and add a test before the new migration is prepared.

The nonsecret records are `docs/SUPABASE_QUALIFICATION.md` and `docs/PROVIDER_PROVISIONING_STATUS.md`. They must remain consistent with this actual state.

## 5. Implemented application contracts and source map

The foundation contains real policy and decision code but does not contain a live provider adapter. Maintain this separation. In particular, `gpuDispatchService.ts` is intentionally a pure server-side admission/reservation/settlement contract, not a connection to Supabase with privileged credentials and not a RunPod client.

| Path | Purpose | Continuation caution |
|---|---|---|
| `shared/activationPolicy.ts` | Authoritative flags for `customerIntake`, CDR, OCR/GPU, and promotion | Keep every flag `false` until separate evidence and contextual approval |
| `shared/tenantDomain.ts` | Roles, tenant entities, document/candidate states | Preserve tenant vocabulary across all later adapters |
| `shared/creditEconomics.ts` | Credit packs, rate mapping, limits, margin calculation | Assumptions are non-live economics inputs; recalibrate only from approved synthetic metrics |
| `server/foundation/tenantAuthorization.ts` | Membership and entitlement/quota evaluation | Server-side only; no browser trust assumption |
| `server/foundation/uploadCapability.ts` | Upload admission check | Must deny immediately while global intake policy is false |
| `server/foundation/r2UploadContract.ts` | Direct-to-R2 capability shape | Must not emit a live signed URL without approved foundation signer |
| `server/foundation/quarantineUploadCompletion.ts` | Post-upload metadata-only completion check | Requires issued capability plus exact object key, length, source digest, and declared/observed MIME; it never reads browser or R2 bytes |
| `shared/qualifiedDocumentInputs.ts` | Future CDR-compatible input metadata allowlist | Accepts only declared MIME/filename pairs supported by the Foundation-local CDR; the CDR must still independently inspect bytes |
| `server/foundation/paddleCheckout.ts` | Server-controlled price lookup | Price IDs intentionally remain null; no arbitrary browser price ID |
| `server/foundation/paddleWebhook.ts` | Raw body HMAC, timestamp, event-id dedupe, occurred-at ordering | Do not alter to parse a body before signature verification or to credit checkout success redirects |
| `server/foundation/supabaseAuth.ts` | Auth readiness and configuration validity contract | No fallback value, existing production client, or old OAuth client may be used |
| `server/foundation/gpuDispatchService.ts` | Reservation, idempotency replay, settlement/release, operator review and margin pause | Defaults to disabled; never introduce silent retries or direct GPU execution |
| `shared/runpodSyntheticQualification.ts` | Synthetic endpoint-create preflight | Source-only safety policy: requires release/capacity evidence, non-SSH port-8001 health, zero workers/volume, 80 GiB disk, $5 cumulative ceiling, and no paid-write replay |
| `shared/documentProcessing.ts` | CDR proof, GPU receipt, and candidate human-review metadata binding | Never retrieves bytes or invokes a provider; proof/receipt must remain digest- and workspace-bound, and approval never enables promotion |
| `server/routers.ts` | Safe tRPC read-only exposure of readiness/policy/CDR preflight/credit catalog | Do not expose privileged activation mutation without a later approved design |
| `supabase/migrations/` | Product domain DDL/RLS | Apply only reviewed future migrations to `tfcorhjkqcuisqhsjemz` after scope approval |
| `nextjs/` | Standalone Next.js App Router/Vercel package | Deployment package only; no Vercel project or secrets are configured |
| `docs/CDR_BOOTSTRAP_APPROVAL_PACKAGE.md` | Separate one-shot CDR bootstrap record | This is outside normal foundation work and is not a command to execute |

The UI lives primarily in `client/src/pages/Home.tsx` and the workspace route. The current uncheckpointed landing page includes a three-pack prepaid credit presentation—Starter `$12 / 100 credits`, Builder `$30 / 300 credits`, and Scale `$75 / 800 credits`—with explicit “no unlimited GPU plans” messaging. Each control only shows a notice that the pack is staged for Paddle sandbox; it must not produce payment or capacity. **Verify this current UI change by test and screenshot before calling it complete or checkpointing it.**

## 6. Pricing, Paddle, and GPU-cost governance

The agreed product direction is a **hybrid**: subscription access manages workspaces/governance; prepaid credits fund GPU-bound processing. There are no recurring included GPU credits. This intentionally keeps exposure bounded when a high-cost task is submitted and creates a clear entitlement boundary between access and compute.

| Non-live proposed pack | Credits | Gross price | Purpose |
|---|---:|---:|---|
| Starter | 100 | $12 | Low-risk entry and qualification cohort |
| Builder | 300 | $30 | Default team purchase |
| Scale | 800 | $75 | Higher-volume prepaid capacity |

Using the previously researched Paddle fee assumption of 5% plus $0.50, the smallest pack yields about `$0.0884` net per credit. With a maximum GPU allocation target of 25%, the gross cost ceiling is about `$0.0221` per credit. The draft rate mapping is one credit per 45 seconds on a 4090, 18 seconds on an A100, or 12 seconds on an H100. These are initial policy assumptions, not published promises. They must be recalibrated only from approved synthetic P50/P95 job-cost evidence after upstream sanitation and R2 qualification.

The GPU policy is intentionally restrictive: Flex starts at zero active workers and a maximum of one; GPU class cannot automatically upgrade; a job reserves 2–10 credits; workspace daily consumption is capped at 20 credits; execution timeout is at most 90 seconds; reservation TTL is at most 300 seconds; idempotency is mandatory; ambiguous/over-reserved execution goes to manual review; and a margin-floor breach pauses future dispatch. RunPod’s documentation states that serverless capacity bills worker active time and supports per-request execution policy overrides, so all final policy values must be server-owned and never taken from a browser request.[1] No RunPod endpoint, account credential, worker, or job exists.

Paddle remains wholly unconfigured. The browser did not have an authenticated Paddle vendor session, and no vendor account, products, prices, checkout, notification destination, signature secret, sandbox event, or live event exists. Until a user-approved sandbox configuration is completed, every checkout attempt must stay at `BILLING_NOT_CONFIGURED`. The webhook design uses HMAC-SHA256 on the unmodified raw body in the form `ts:raw_body`, constant-time signature comparison, bounded clock tolerance, event ID deduplication, and `occurred_at` ordering. This follows Paddle’s webhook verification and delivery guidance.[2] [3]

> **Billing invariant:** A browser return from a checkout success URL is never proof of payment and can never issue an entitlement or credit. Only a verified server-side Paddle notification may project billing state.

## 7. Cloudflare R2 status and customer-byte boundary

The only Cloudflare resource created for this foundation is the separately named R2 bucket `tavonel-saas-foundation-quarantine`. The provider reports Standard storage, `APAC` location, and default jurisdiction. APAC is a **best-effort regional placement label**, not a Korea data-residency guarantee. Do not represent it as Korean residency in UI, contracts, documentation, or sales copy.

No Worker binding, CORS configuration, public endpoint, API token, access key, signing identity, or retained canary object exists. A single approved 69-byte ASCII marker was PUT, read back at the same key, and immediately deleted with HTTP 200 for each operation. That transient test touched only the exact Foundation bucket, disclosed no body/credential, and left no object behind. The bucket is not linked to the application. The browser dashboard session was unauthenticated; bucket creation used a separate authenticated account binding and did not visit/change `tavonel-prod-quarantine` or any production Cloudflare object. Details are in `docs/SYNTHETIC_R2_QUALIFICATION_2026-08-27.md`.

The only permitted future first step is a harmless synthetic canary after a separate contextual approval specifically covers: the strict APAC disclosure, a new foundation-only least-privilege signing identity, server/Worker custody of the credential, expected document-key/mime/size binding, short expiry, and the fact that an externally callable upload capability is being created. A signed URL must be presigned server-side and must not expose signing credentials to the browser.[4] Even after a harmless canary, `customerIntake` stays false until the entire security chain is approved.

## 8. CDR status and one-shot bootstrap boundary

The CDR process is not part of an enabled Foundation runtime. The source activation repository remains isolated and unchanged by Foundation work. Its audited commit `e017cb65b8dd0a666740aa53a671a4ae10171dda` was copied into `quarantine-sidecar/cdr-cloudrun/` in this private Foundation repository, with only the future bootstrap project identifier changed to `tavonel-saas-foundation`; provenance is recorded in `quarantine-sidecar/cdr-cloudrun/FOUNDATION_SOURCE_PROVENANCE.md`. The copied service passed 10 harmless fixture tests locally. Docker is unavailable in the sandbox, so no image build was attempted.

The existing production Seoul Cloud Run service `tavonel-pdf-cdr` is audit-only evidence: its `GET /health` reports `CDR configuration is not qualified`. It has not been modified or invoked for sanitization. A separate Google Cloud project, `tavonel-saas-foundation`, is the only permissible future Cloud Run target. Google granted the one-project paid-services quota and, with the user's explicit approval, the existing active billing account was attached only to this Foundation project. Cloud Run Admin API (`run.googleapis.com`), Secret Manager API, and Cloud Build API were then enabled only in that Foundation project. A reviewed Foundation-only Cloud Build created `tavonel-cdr-hmac` with user-managed `asia-northeast3` replication; the payload was never read or logged, and Cloud Run remains empty. This still does not authorize a Cloud Run service, Artifact Registry resource, CDR request, GPU resource, or customer data. The next step requires separate final CDR deployment confirmation.

The production project’s Developer Connect connection, service account, source repository, and bootstrap package are not inputs to the Foundation path. Do not reuse, inspect, repair, or bypass them. If the Foundation billing link becomes available, first inspect the Foundation-local Dockerfile and `cloudbuild.bootstrap-cdr-secret.yaml` at the then-current private `main` revision. A deliberate one-off build may be considered only from that Foundation source; do not create a push trigger, polling loop, broad source connection, or scheduled retry.

| Future Foundation bootstrap preflight | Required condition |
|---|---|
| Source tree | Current private `0ssol1620-byte/tavonel-saas-foundation` `main`, with the exact commit recorded at execution time |
| CDR provenance | Foundation-local copy records audited source commit `e017cb65b8dd0a666740aa53a671a4ae10171dda`; no production source connection is reused |
| Target project and region | Only `tavonel-saas-foundation` in `asia-northeast3` (Seoul) |
| Billing and API state | Foundation-only billing link and Cloud Run API propagation are read-only confirmed first |
| Runtime ceiling | Gen 1; min instances `0`; max instances `1`; concurrency `1`; 1 vCPU; 2 GiB; 120-second timeout; request-billed; access logging disabled |
| HMAC material | New Foundation-only value generated inside provider infrastructure and retained solely in Foundation Secret Manager/runtime attachment; no prior value is reused or exposed |

The Foundation one-shot bootstrap was submitted once after billing/API preflight and succeeded. It created exactly one version of the Foundation HMAC secret through the reviewed Foundation-local YAML, with material generated only inside provider infrastructure and no secret emitted in source, arguments, or logs. It introduced no trigger, schedule, retry, substitution, or inline secret script. Success does not approve Cloudflare secret synchronization, runtime secret attachment, customer request execution, OCR/GPU work, or candidate promotion. `docs/CDR_BOOTSTRAP_APPROVAL_PACKAGE.md` is historical background only and must never override this Foundation-only table.

## 9. Verification status and exact next commands

The latest full Foundation verification passed **17 Vitest files / 41 tests**, `pnpm check`, `pnpm build`, and `git diff --check`. The package-local Next.js verification passed **2 Vitest files / 2 tests**, `pnpm check`, and `NODE_ENV=production pnpm build`. The copied CDR suite passed **10 harmless fixture tests**. The production environment setting is important: a bare Next build can be misleading in this managed environment because of an atypical `NODE_ENV`; use the explicit production command below.

The latest updates that must be verified before a checkpoint are: qualification/provisioning status documentation, `todo.md` corrections, and `Home.tsx` prepaid-credit presentation. Run commands from a safe shell, never adding secret values to the command line:

```bash
cd /home/ubuntu/tavonel-saas-foundation
pnpm test && pnpm run check && pnpm run build && git diff --check

cd /home/ubuntu/tavonel-saas-foundation/nextjs
pnpm test && pnpm run check && NODE_ENV=production pnpm run build
```

Then capture the desktop and mobile landing page and confirm: the Starter/Builder/Scale cards render; “No unlimited GPU plans” is visible; clicking a pack shows only a staging notice; no checkout, upload, or GPU request occurs; and all existing fail-closed messaging remains comprehensible. Treat a successful compile alone as insufficient UI verification. Check local development logs directly from `.manus-logs/` with terminal commands if an issue arises; do not store a test document. To rerun the separate copied CDR fixture suite, use `cd quarantine-sidecar/cdr-cloudrun && python3 -m unittest discover -s tests -p 'test_*.py' -v` from the Foundation root.

Before saving a new Foundation checkpoint, read all of `todo.md` and ensure that only genuinely incomplete items are unchecked. The outstanding items should include the separately approved Foundation CDR deployment/qualification, Auth configuration, Paddle sandbox, R2 signer, branch-only RLS matrix if a paid plan is separately approved, GPU qualification, and all real intake/processing/promotion gates. The handoff task must be marked complete only after this document is verified. A future checkpoint must describe only Foundation source; it cannot publish or deploy an unrelated existing production project.

## 10. Required order for future external activation

External integration may advance only in this order. Completing one row grants no permission for the next. The user’s broad prior approvals do not substitute for the contextual confirmation shown here.

| Order | Controlled activity | Preconditions / required user confirmation | Must remain excluded |
|---:|---|---|---|
| 1 | Supabase Auth sandbox | Confirm exact foundation HTTPS preview/production origin, redirect URI, dedicated Google OAuth client/consent screen, and secret handling | Existing production OAuth client, customer intake, browser service-role access |
| 2 | Paddle sandbox catalog | User-controlled vendor account/session and explicit acceptance of sandbox terms; confirm one foundation-only endpoint and secret storage | Live products/prices, live checkout, KYC/live merchant activation |
| 3 | Paddle simulator qualification | Server-only secret configured; endpoint uses raw-body verifier; test event evidence reviewed | Credit from redirect, production notifications, automatic retries |
| 4 | R2 synthetic canary | Exact scoped signer/credential model and APAC non-residency wording approved | Customer bytes, public endpoint, CORS broadening, server byte proxy |
| 5 | R2/application chain qualification | Harmless canary metadata/proof reviewed; signer binds tenant/key/mime/size/expiry | Global intake flag, public upload form, production-bucket changes |
| 6 | CDR bootstrap only, if independently needed | Final direct authorization matching the pinned table in §8 and authenticated operator surface | Runtime secret attachment, Cloudflare sync, request execution |
| 7 | Upstream security chain | Synthetic AV/CDR proof, provider controls, and manual review methodology qualified | Customer documents, unqualified third-party scanning, automation/retries |
| 8 | GPU synthetic cost qualification | Explicit GPU/RunPod approval, endpoint/credential under managed secrets, synthetic P50/P95 economics | Unlimited/auto-upgraded GPU, client-driven policy, customer data |
| 9 | Limited pilot intake | Separate final approval after all prior evidence is reviewed | Unattended candidate/world promotion or live/production payment without its own approval |

If Supabase branching is revisited, quote any Pro-plan cost first, obtain a new confirmation, and create only a disposable branch/test project. The previous branch attempt did not create any resource because the provider required Pro. Never upgrade in anticipation of a test.

## 11. Current tracked work and handoff completion procedure

`todo.md` is the canonical task tracker. It has been corrected to distinguish completed Supabase project/migrations/advisor/RLS probe work from future configuration. It should retain rather than erase history. The immediate handoff-completion sequence is:

1. Read this handoff and current `todo.md`, then verify the nonsecret state against `docs/SUPABASE_QUALIFICATION.md`, `docs/PROVIDER_PROVISIONING_STATUS.md`, and `docs/CDR_BOOTSTRAP_APPROVAL_PACKAGE.md`.
2. Perform the local parent and Next.js verification commands in §9.
3. Visually validate that the new credit cards remain presentation-only at desktop and mobile widths.
4. Mark the handoff TODO item as complete, but do not close real provider or live-path TODOs.
5. Save a new checkpoint and attach only that checkpoint reference when reporting completion.
6. Ask for a precise contextual confirmation before initiating any provider-side activity beyond read-only inspection.

## 12. Private GitHub cross-account transfer

The isolated foundation source is now available in the private GitHub repository **[`0ssol1620-byte/tavonel-saas-foundation`](https://github.com/0ssol1620-byte/tavonel-saas-foundation)**. The repository’s default branch is `main`, which is the source of truth for the continuation revision. A clean shallow clone contained the root package, `nextjs/package.json`, and this handoff file, had a clean working tree, and had no tracked file whose name indicated an environment file, private key, credential, or secrets directory.

The repository is **private**. Before another Manus account can use it, the GitHub owner must invite the GitHub account that the other Manus session uses as a repository collaborator (GitHub repository → **Settings** → **Collaborators and teams** → **Add people**), and that account must accept the invitation. Do not change the repository to public merely to simplify handoff.

After the invite is accepted, the other account’s session should clone only this repository and work only inside the cloned directory. In that **fresh clone**, `origin` is the private GitHub repository and is safe to use for normal pull/push after collaboration is authorized. Only the original managed workspace had a distinct artifact remote named `origin` and an added GitHub remote named `github`; that naming distinction does not carry into a fresh clone. Do not add, reconfigure, or delete remotes in either workspace without understanding the distinction.

```bash
gh repo clone 0ssol1620-byte/tavonel-saas-foundation
cd tavonel-saas-foundation
git status --short
git rev-parse HEAD
```

After cloning, compare `git rev-parse HEAD` to GitHub’s displayed `main` revision or run `git ls-remote origin refs/heads/main`. The continuing session must read this handoff and `todo.md`, run the parent and Next.js verification commands in §9, and preserve every fail-closed/approval condition in this document. It must not use this repository as authority to touch production or the isolated CDR activation repository.

## 13. Reference material

The following sources define provider-specific behavior used by the design. They should be consulted again if the corresponding provider integration moves from contract to approved implementation.

[1]: https://docs.runpod.io/serverless/overview "RunPod Serverless overview"
[2]: https://developer.paddle.com/webhooks/about/signature-verification "Paddle — Verify webhook signatures"
[3]: https://developer.paddle.com/webhooks/about/respond-to-webhooks "Paddle — Handle webhook delivery"
[4]: https://developers.cloudflare.com/r2/objects/upload-objects/ "Cloudflare R2 — Upload objects and presigned URLs"
[5]: https://supabase.com/docs/guides/database/postgres/row-level-security "Supabase — Row Level Security"

---

## Compact continuation prompt for a new Manus session

> Continue only inside `/home/ubuntu/tavonel-saas-foundation`. Read `docs/HANDOFF_2026-08-27.md`, `todo.md`, `docs/SUPABASE_QUALIFICATION.md`, `docs/PROVIDER_PROVISIONING_STATUS.md`, and `docs/CDR_BOOTSTRAP_APPROVAL_PACKAGE.md` first. Do not touch existing TAVONEL production or the isolated activation repository. The only dedicated Supabase target is `tfcorhjkqcuisqhsjemz` in Seoul; four migrations are applied, 13 tables are RLS-enabled/empty, synthetic rollback RLS probe passed, and final Advisor is clean. R2 foundation bucket exists but has no signer; Paddle/Auth/RunPod/Vercel deployment are unconfigured; all intake/CDR/OCR/GPU/promotion remain false. Run parent and Next.js tests/builds, visually validate the new prepaid-credit presentation mode, then save a checkpoint newer than `7becd846`. Require a fresh contextual confirmation for any credential, OAuth, external plan/cost, merchant terms, R2 signer/CORS, deploy, payment, real data, CDR build, GPU endpoint, or live intake.
