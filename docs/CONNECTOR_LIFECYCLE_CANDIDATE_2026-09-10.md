# Connector lifecycle candidate

Status: implemented candidate; Google reader is not selected by the production worker.

The current worker treats non-file events as unsupported imports. A removal event can therefore be skipped while the job advances its cursor and reports success. The candidate worker stops with `SOURCE_LIFECYCLE_REVIEW_REQUIRED` before importing a remaining page containing removals. It does not advance that checkpoint. This is an explicit operator failure, not revocation of an existing source or World.

The Google Drive reader takes a change watermark before enumeration and returns a separate zero-item checkpoint. Its caller must persist that checkpoint before fetching the first snapshot page. It replays changes from the original watermark after enumeration, retaining the final change token for subsequent polling. Metadata version identifies rename changes. Removals distinguish observed trash from deletion-or-loss-of-access ambiguity. Missing, malformed, incomplete, or stalled provider responses are refused.

Legacy cursors are not converted or reset automatically. Shared-drive lifecycle events are refused for review. Existing imported data is not retroactively assigned a guessed native identity.

The import path now emits a stable logical source ID scoped by workspace, connection, provider and native ID, plus a revision-specific source version ID. Names and paths do not participate. Existing valid quarantine document IDs remain byte-for-byte compatible. Missing or control-character-bearing native IDs/revisions fail before downloading; they cannot create ambiguous delimiter-based replay identities.

Migration `20260909193323_connector_document_bindings.sql` and the binding store persist this identity, document ID, observed byte digest, length and MIME before intake admission. Replay reads and compares the stored winner; a different digest under the same provider revision refuses. The database assigns the original recorded timestamp. Its trigger verifies an active same-workspace/provider connection and locks it during insertion. Bindings cannot be updated or deleted and have no anonymous/authenticated access grants. A binding records an observation, not successful quarantine/CDR/compilation.

Deployment order: rehearse and apply the additive migration before deploying the import change. A missing table fails closed as CONNECTOR_BINDING_WRITE_FAILED. This candidate migration has only been applied to a disposable local PostgreSQL fixture; production is unchanged. The full repository migration chain is still a CI gate. Binding persistence is not retrieval authorization or removal propagation.

The existing source-domain store cannot simply be called with these identities: its quarantine key check assumes sourceId equals documentId, and its exact insert replay compares observation timestamps. Connector integration must introduce an explicit immutable document binding and stable observation replay semantics rather than weakening workspace key validation or overwriting timestamps. This remains a required implementation step.

## Remaining integration gates

Candidate addition: `connector_source_suspensions` stores an immutable denial for a bound source. The sync worker records known removal events before keeping the job in review; unknown/unbound events remain unresolved. The retrieval pipeline checks bound source suspension and connection status before external reranking and again before returning context. This is a negative authorization overlay, not complete per-user ACL evaluation: unbound legacy imports, group membership, provider permission capture, fallback Ask, downloads/exports and revocation of already issued packages remain separate work. No automatic unsuspension is implemented.

Both connector migrations must precede the app deployment. Missing authorization RPC fails retrieval closed. Actual production migrations remain unapplied. Local PostgreSQL tests cover active allowance, suspended denial, connection-revoked denial, tenant scope, browser denial and immutable suspension; mocked pipeline tests prove no reranker call for denied sources and no context return after mid-rerank denial. Full migration-chain CI is still required.

Signed collection download now checks package source manifest IDs against the RAG document inventory, refusing missing, duplicate or mismatched inventories. The direct route and its v1 wrapper check source access before signing and again after lease cleanup and account revalidation, before constructing the ZIP response. Unit route tests deny both early and late source revocation without ZIP bytes. This does not retract a package already downloaded. Fallback Ask and cached answer replays still need equivalent source checks.

Subsequent implementation: Ask now validates active source inventory before new/fallback/cached answers and checks suspension before cache completion and after lease cleanup. Authenticated World loading, raw collection JSON and new PDF read-capability issuance also check source denial. Public deterministic buildWorldReadModel samples remain pure and do not call private authorization. Already-issued PDF capabilities still live for up to 120 seconds; strict immediate revocation requires a separately authorized streaming path. Existing downloadable packages cannot be recalled. Full per-user ACL and legacy import qualification remain open.

DB repair replay: full-chain run34397664300 passed both initial application and repair replay after 0053 was updated to restore SELECT/INSERT for the two known connector tables when they exist. This CI receipt covers commit0ff99ab, not later application changes.

PDF candidate update: source metadata now names an authenticated same-origin API route, not an R2 bearer capability. The `format=pdf` path bounds the R2 read to32 MiB, verifies its immutable SHA256, then rechecks user, product and source access before streaming any bytes. The Studio sends its bearer token only in the API request header and passes received bytes directly to PDF.js; no capability or token is placed in a URL. Existing pre-deployment capabilities expire on their original schedule; already-received bytes cannot be recalled. Final viewer/browser, large-file platform streaming and cost/concurrency qualification remain required before promotion. Streaming follows Vercel's response-size guidance: https://vercel.com/kb/guide/how-to-bypass-vercel-body-size-limit-serverless-functions

1. Persist workspace + connection + provider + native ID bindings and revision observations; support Dropbox path aliases without conflating distinct files.
2. Apply tombstones and permission shrink to all relevant imported versions and retrieval/export authorization before advancing the provider checkpoint.
3. Bind source updates to affected Worlds and expose stale/review states; never leave a removed source usable because the sync stopped.
4. Persist checkpoints across jobs, reconcile legacy inventories, and exercise retry/crash/concurrent-worker behavior against the real database.
5. Qualify create, update, rename, move, delete, permission shrink, revoke, reconnect, and expired-cursor recovery using actual test accounts. Keep Beta and the customer-data gate until these checks pass.

Validation: 85 focused tests passed, including 20 Google reader tests and 20 worker tests. Typecheck and lint passed. These mocks prove control flow and parsing, not live provider qualification or production ACL enforcement.

Primary contracts:
- https://developers.google.com/workspace/drive/api/guides/manage-changes
- https://developers.google.com/workspace/drive/api/reference/rest/v3/changes
- https://developers.google.com/workspace/drive/api/reference/rest/v3/files

PDF resource guard candidate: the binary source route acquires the existing workspace export lease before loading PDF bytes. The stream retains that slot through consumption or cancellation; early provider refusal, thrown failure and post-load authorization denial release it through finally. Response ownership transfers only after Response construction. Focused tests cover those boundaries and the existing export cap. This bounds PDF loading/streaming; the preliminary workspace object listing is still before acquisition. The 75-second crash-recovery lease relies on the deployed route enforcing its 30-second execution limit. Actual large-file Vercel streaming and authenticated browser qualification remain open. Independent read-only candidate review found no concrete surviving defect; this is not production verification.

Durable page observation candidate (2026-09-10): `connector_sync_page` stores the first provider page under workspace/job/connection/target/cursor identity before imports. Every resumed offset reads that stored page; it never skips an offset into today's changed provider listing. The SQL function locks and checks the current unexpired job lease plus active connection, and first observation wins. Browser roles cannot invoke it; the new table exposes only SELECT/INSERT to service_role, with RLS and composite job/workspace FK. Snapshots are removed with their parent job by cascade. A legacy partial cursor without an observed page is explicitly REVIEW_REQUIRED, not silently restarted. Store outages retry without import or cursor advancement.
Migration `20260909202224_connector_sync_page_snapshots.sql` is now a THIRD required connector migration before app deployment. 0053 repair replay restores its exact SELECT/INSERT grants. Local PostgreSQL rehearsal covers initial miss, save, first winner, wrong worker/workspace, expired lease and browser denial. This does not qualify full provider lifecycle: cross-job provider checkpoint reuse, permission capture, deleted-path aliases, revision-to-downloaded-byte binding and live provider tests remain open. Full-chain exact-head CI must pass before promotion; no production migration applied.

Dropbox revision-to-byte candidate (2026-09-10): downloads now use the exact `rev:` reference, then require response metadata ID/revision to match the listed immutable ID/revision. Metadata size and Dropbox's block-based content_hash must match the bounded bytes before recordConnectorDocumentBinding or intake reservation. Missing proof, another version, or altered content is refused. Tests include empty/single/multi-block hash vectors independently computed with .NET SHA256, matching and mismatching provider metadata, and actual import-path ordering. This is mock-provider qualification only; live Dropbox account/revocation/deletion tests remain open. Google Drive and Microsoft revision-to-byte binding remain unfinished.
Official references: https://developers.dropbox.com/dbx-file-access-guide and https://www.dropbox.com/developers/reference/content-hash . No proprietary routing information is published by this change.
Snapshot migration full-chain CI: run34401229941 at f9acba9 passed db-rehearsal (2m53s), including the repository repair-replay workflow. Later source changes require their own exact-SHA checks. No production schema/application promotion occurred.

Google/Microsoft version-observation candidate (2026-09-10): the import path now reads bounded provider metadata before and after download, compares identity/version/MIME/size/checksum observations, and stops before binding/intake if metadata changes or access disappears. Google binary files require MD5 or SHA256 matching the bytes; Google native exports have version/download-permission bracketing but no provider export-body checksum and are not claimed to have cryptographic version proof. Legacy timestamp-only revisions are refused. Google media requests include supportsAllDrives.
Microsoft metadata and content now share one target resolver, including siteId; previously content requests ignored siteId and used me/drive. eTag/cTag bracket the download and SHA1 or QuickXorHash compares the bytes. Official Graph documentation marks sha256Hash unsupported, so it is deliberately not used. QuickXorHash is non-cryptographic; stored artifact SHA256 remains separate. Its bit-vector fixtures were independently evaluated with.NET BitArray, including160-byte wrap. Missing supported hashes fail closed.
Primary references: https://developers.google.com/workspace/drive/api/reference/rest/v3/files ; https://learn.microsoft.com/en-us/graph/api/driveitem-get?view=graph-rest-1.0 ; https://learn.microsoft.com/en-us/graph/api/resources/hashes?view=graph-rest-1.0 ; https://learn.microsoft.com/en-us/onedrive/developer/code-snippets/quickxorhash?view=odsp-graph-online . Initial incorrect /graph/resources/hashes URL was replaced with the actual /graph/api/resources/hashes page.
Still not qualified: provider consistency under real concurrent edits, fresh account permission lifecycle, native export fidelity, legacy content-hash revision migration, cross-job checkpoints, full customer E2E. Two extra bounded metadata reads add latency; batch/deadline qualification remains required. No production promotion or Beta removal.

## Listing integrity — 2026-09-10 05:57 KST
The actual provider adapters now refuse invalid JSON/non-object responses, absent or malformed item arrays, and rows that cannot be normalized. Previously these could be dropped or treated as an empty completed page. Google continuation values are checked before returning a page; Dropbox requires a boolean has_more and a retained valid cursor even at completion; explicit invalid Graph continuation values refuse. The worker preserves these error codes and retries without advancing a checkpoint or importing any bytes.
Verified: full212files/2587tests passed; pnpm check passed. Initial full run found3 contract assertions encoding the old silent-drop success behavior; replaced those with explicit refusal expectations while retaining hostile payload/prototype checks. Both failure and final receipts retained: .chatgpt2codex/connector-page-integrity-full-unit-0910.log and connector-page-integrity-full-unit-final-0910.log. Real providers not exercised. This intentionally refuses unqualified empty/missing envelopes rather than assuming an empty source; any provider's documented omission variant needs explicit qualification.
Still open: Google selected-target enforcement in current files reader (target is ignored), lifecycle reader selection, cross-job checkpoint handoff, complete Graph terminal delta evidence, per-user ACL and real provider verification. The stricter parser does not implement these. No production deployment/schema change/customer activation. Full program goal remains active.

## Google target fidelity — 2026-09-10 06:01 KST
Actual files reader now applies selected shared-drive ID with corpora=drive, supportsAllDrives and includeItemsFromAllDrives on first and continued requests. Default corpus is user. Nonempty Dropbox rootPath or Graph siteId in a Google request, and invalid/empty drive IDs, refuse before network rather than broadening to all files. This does not implement Google folder-tree traversal. The UI currently has no verified Google target picker; do not imply an existing folder-selection UX was qualified.
Requested incompleteSearch and refuse any non-false reported incompleteness. Identical next token refuses instead of replaying a saved page forever. Corrected prior06c5760 refusal of nextPageToken:null: Google's current search guide documents null as completion; null and absent are both accepted, malformed non-null tokens still refuse. Official references: https://developers.google.com/workspace/drive/api/guides/search-files and https://developers.google.com/workspace/drive/api/guides/enable-shareddrives .
VERIFIED: focused86passed, check passed, full212files/2591tests passed. Receipt .chatgpt2codex/drive-target-full-unit-0910.log. Provider API calls in tests are simulated, not consent or live source qualification. No production schema/deploy.
NEXT: enqueue_foundation_job currently starts a new job without a previous completed checkpoint. Its connection-only live dedup can return an old target's job for a different target. Implement atomic target-bound checkpoint admission with serialized connection/target state and reader-version binding; do not read last job then write non-atomically. Legacy Google listing terminal cursors are not change watermarks and must never be reused as one. Durable Google folder traversal and lifecycle-reader selection remain unfinished, along with the existing full goal gates.

## Atomic cross-job checkpoints — 2026-09-10 06:10 KST
Implemented new enqueue_connector_sync RPC and actual sync POST integration. Connection-row locking serializes admission; identical target/reader joins an active job, different target or legacy reader returns JOB_SYNC_CONFLICT (HTTP409). Active connection workspace must match. Terminal successful Dropbox/Graph jobs atomically update a separate checkpoint table in the same transaction; next job reads target/reader-bound cursor under the connection lock. Job history deletion does not erase this watermark. Google files-v1 jobs deliberately do not seed a change checkpoint; lifecycle reader is still unselected. Graph listings now require an actual nextLink or deltaLink, not a missing terminal link. Malformed POST JSON refuses instead of defaulting to an all-source target.
Fourth required migration:20260909210203_connector_sync_checkpoints.sql. Three earlier PR51 migrations remain required. None applied to production. New table RLS + service SELECT/INSERT/UPDATE only; browser table/RPC denied;0053 repair and grant matrix include it. Trigger is invoker. Reader versions are chosen by database provider, never by HTTP input.
Actual PostgreSQL17 rehearsal discovered an existing0013 constraint defect: regex{3,500} raises invalid repetition count on connection insertion. New migration preserves allowed schemes/chars and3..500 suffix bounds with an unbounded regex plus separate length predicate. Synthetic500-char success,501-char and2-char rejection verified.
VERIFIED: full212files/2598tests and check passed. Local DB connector_checkpoint_0910 on dedicated55441 uses actual connection table DDL +0024/0025/0029/newmigration and minimal auth.users fixture, NOT full Supabase migration chain. Actual tests: same-target dedup; different-target conflict; tenant refusal; success checkpoint; next-job continuation; invalid cursor rolls back job success and preserves prior checkpoint; different target starts fresh; browser grants/RLS. Two concurrent psql sessions yielded one created:true, one created:false, same job ID and live_jobs=1. Receipts .chatgpt2codex/checkpoint-db-final-0910.log, checkpoint-concurrent-a.sql/b.sql and checkpoint-concurrency-receipt-0910.json. Initial incomplete bootstrap and initial full-unit fixture-count failure retained; pgTAP assertion now checks actual browser RPC denial. Full CI DB chain pending.
RELEASE/ROLLBACK: migrations first with dispatcher quiesced and old worker invocations drained; do not let old worker code execute new versioned jobs. Rollback must pause new admissions/dispatch and drain or safely park versioned jobs before older app runs. customerData remains OFF. Provider token expiry/rebaseline, manual reset/delete controls, Google folder traversal/lifecycle selection, immutable source/ACL lifecycle and actual providers remain unqualified. This is implemented candidate behavior, not completed connector production qualification or full goal completion.
