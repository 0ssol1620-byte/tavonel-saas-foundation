# Local Verification Record — 2026-08-27

## In progress

The Foundation landing page is being re-validated against `docs/HANDOFF_2026-08-27.md` §9 after a clean private-repository clone and lockfile-based dependency installation. No provider configuration, deployment, credential, customer byte, payment, CDR, OCR/GPU, or candidate-promotion action has been performed.

## Desktop landing observation

The locally served landing page rendered its private-pilot hero, four-step integrity chain, and explicit foundation-mode notice that no document bytes are accepted. Its document-visible content included the non-live `Starter` ($12 / 100 credits), `Builder` ($30 / 300 credits), and `Scale` ($75 / 800 credits) presentation, together with the statement that there are no unlimited GPU plans.

The pricing navigation action required a viewport wait before lower sections became visible. The completed desktop check confirmed that the three recurring access cards and the three prepaid-credit cards each render with their expected hierarchy and contrast. The `Starter` preview-pack control produced the explicit notice that it is staged for Paddle sandbox only and that no payment session or GPU capacity is created. No route change, checkout, upload, job dispatch, or other live provider action occurred.

The mobile viewport check remains pending. This record establishes no live checkout, upload, or processing capability.

## Mobile landing observation — partial

The first two vertically tiled mobile screenshots (390 px wide) show a legible two-control navigation treatment, a non-truncated hero, full-width primary and secondary controls, the foundation-mode byte-acceptance denial, and a single-column four-step integrity chain. The quarantine, sanitize, and understand steps visibly remain `held`; review remains the only human-governance state. The remaining mobile sections, including access plans and prepaid packs, are still being inspected in source order.

The next two tiles show the Method section continuing as readable, bounded single-column cards. Its wording preserves the stated data boundary: large documents bypass the application and database, a candidate cannot become an unattended conclusion, and the sanitization proof remains part of visible lineage.

The following two tiles show the pricing header and its `presentation-only` prerequisite copy, an accessible Monthly/Annual control, and responsive single-column access-plan cards. The Observer plan content and the beginning of the Studio card are readable without horizontal overflow or truncated pricing information.

The next two tiles confirm that the Studio and Institution cards retain hierarchy, contrast, button reachability, and complete feature lists on mobile. The following Deliberate Compute section repeats the bounded-compute invariant before the prepaid-card list begins; it does not introduce a live GPU action.

The final mobile region confirms that Starter ($12 / 100 credits), Builder ($30 / 300 credits), and Scale ($75 / 800 credits) render as complete, separated, full-width cards with reachable `Preview pack` controls. The final “No unlimited GPU plans” guardrail and footer explicitly retain foundation-mode/no-live-intake wording. Together with the desktop interaction check, this completes the requested visual verification of the prepaid-credit presentation without enabling a checkout, upload, or GPU path.

## Trial-credit presentation update

After the trial-credit contract was added, the mobile compute region rendered the new one-time **2-credit, 7-day verified-signup trial** statement above the full Starter, Builder, and Scale card sequence. The statement continues to say issuance is unavailable until the controlled processing path is qualified. This is a policy presentation and not a live credit grant.
